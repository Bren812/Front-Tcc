import { useState, useEffect } from "react";
import { Search, Package, Users, Building2 } from "lucide-react";
import { Product, User, Supplier, Category, UnitOfMeasure } from "../../types"; type ViewType = 'products' | 'users' | 'suppliers'; export default function List() { const [viewType, setViewType] = useState('products'); const [searchTerm, setSearchTerm] = useState(""); const [products, setProducts] = useState<Product[]>([]); const [users, setUsers] = useState<User[]>([]); const [suppliers, setSuppliers] = useState<Supplier[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [units, setUnits] = useState<UnitOfMeasure[]>([]); useEffect(() => { setProducts(JSON.parse(localStorage.getItem("products") || "[]")); setUsers(JSON.parse(localStorage.getItem("users") || "[]")); setSuppliers(JSON.parse(localStorage.getItem("suppliers") || "[]")); setCategories(JSON.parse(localStorage.getItem("categories") || "[]")); setUnits(JSON.parse(localStorage.getItem("units") || "[]")); }, []); const getCategoryName = (id) => categories.find(c => c.id === id)?.name || "N/A"; const getUnitName = (id) => units.find(u => u.id === id)?.abbreviation || "N/A"; const getSupplierName = (id?) => id ? suppliers.find(s => s.id === id)?.name || "N/A" : "N/A"; const filteredProducts = products.filter(p => p.name.toLowerCase().includes(searchTerm.toLowerCase()) || p.code.toLowerCase().includes(searchTerm.toLowerCase()) ); const filteredUsers = users.filter(u => u.fullName.toLowerCase().includes(searchTerm.toLowerCase()) || u.username.toLowerCase().includes(searchTerm.toLowerCase()) ); const filteredSuppliers = suppliers.filter(s => s.name.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="p-8"> <div className="mb-8"> <h1 className="text-3xl font-bold text-gray-800 mb-2">Lista de Insumos</h1> <p className="text-gray-600">Visualize todasções cadastradas</p> </div> <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-6"> <div className="flex gap-2 mb-6"> <button onClick={() => setViewType('products')} className={`flex items-center gap-2 px-4 py-2 rounded-lg font-medium transition-colors ${ viewType === 'products' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > <Package size={20} /> Produtos </button> <button onClick={() => setViewType('users')} className={`flex items-center gap-2 px-4 py-2 rounded-lg font-medium transition-colors ${ viewType === 'users' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > <Users size={20} /> Usuários </button> <button onClick={() => setViewType('suppliers')} className={`flex items-center gap-2 px-4 py-2 rounded-lg font-medium transition-colors ${ viewType === 'suppliers' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > <Building2 size={20} /> Fornecedores </button> </div> <div className="relative mb-6"> <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={20} /> <input type="text" placeholder="Pesquisar..." value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none" /> </div> {viewType === 'products' && ( <div className="overflow-x-auto"> <table className="w-full"> <thead className="bg-gray-50 border-b border-gray-200"> <tr> <th className="text-left px-4 py-3 font-semibold text-gray-700">Código</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Produto</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Categoria</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Estoque</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Mín.</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Fornecedor</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Status</th> </tr> </thead> <tbody> {filteredProducts.map(product => { const isLow = product.lowStockAlert && product.currentQuantity <= product.minStock; return ( <tr key={product.id} className="border-b border-gray-100 hover:bg-gray-50"> <td className="px-4 py-3 text-gray-800">{product.code}</td> <td className="px-4 py-3 font-medium text-gray-800">{product.name}</td> <td className="px-4 py-3 text-gray-600">{getCategoryName(product.categoryId)}</td> <td className="px-4 py-3 text-gray-800"> {product.currentQuantity} {getUnitName(product.unitId)} </td> <td className="px-4 py-3 text-gray-600">{product.minStock}</td> <td className="px-4 py-3 text-gray-600">{getSupplierName(product.supplierId)}</td> <td className="px-4 py-3"> {isLow ? ( <span className="px-3 py-1 bg-yellow-100 text-yellow-700 rounded-full text-sm font-medium"> Baixo </span> ) <span className="px-3 py-1 bg-green-100 text-green-700 rounded-full text-sm font-medium"> Normal </span> )} </td> </tr> ); })} </tbody> </table> </div> )} {viewType === 'users' && ( <div className="overflow-x-auto"> <table className="w-full"> <thead className="bg-gray-50 border-b border-gray-200"> <tr> <th className="text-left px-4 py-3 font-semibold text-gray-700">Nome</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Usuário</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Tipo</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Status</th> </tr> </thead> <tbody> {filteredUsers.map(user => ( <tr key={user.id} className="border-b border-gray-100 hover:bg-gray-50"> <td className="px-4 py-3 font-medium text-gray-800">{user.fullName}</td> <td className="px-4 py-3 text-gray-600">{user.username}</td> <td className="px-4 py-3"> {user.isMaster ? ( <span className="px-3 py-1 bg-purple-100 text-purple-700 rounded-full text-sm font-medium"> Master </span> ) <span className="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-sm font-medium"> Usuário </span> )} </td> <td className="px-4 py-3"> {user.status ? ( <span className="px-3 py-1 bg-green-100 text-green-700 rounded-full text-sm font-medium"> Ativo </span> ) <span className="px-3 py-1 bg-red-100 text-red-700 rounded-full text-sm font-medium"> Inativo </span> )} </td> </tr> ))} </tbody> </table> </div> )} {viewType === 'suppliers' && ( <div className="overflow-x-auto"> <table className="w-full"> <thead className="bg-gray-50 border-b border-gray-200"> <tr> <th className="text-left px-4 py-3 font-semibold text-gray-700">Fornecedor</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Contato</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Telefone</th> <th className="text-left px-4 py-3 font-semibold text-gray-700">Email</th> </tr> </thead> <tbody> {filteredSuppliers.map(supplier => ( <tr key={supplier.id} className="border-b border-gray-100 hover:bg-gray-50"> <td className="px-4 py-3 font-medium text-gray-800">{supplier.name}</td> <td className="px-4 py-3 text-gray-600">{supplier.contact}</td> <td className="px-4 py-3 text-gray-600">{supplier.phone}</td> <td className="px-4 py-3 text-gray-600">{supplier.email}</td> </tr> ))} </tbody> </table> </div> )} </div> </div> );
}
