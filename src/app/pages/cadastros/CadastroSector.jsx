import { useState, useEffect } from "react";
import { Sector, Permission } from "../../types"; const allPermissions: { value; label: string }[] = [ { value, label: 'Visualizar Produtos' }, { value, label: 'Adicionar Produtos' }, { value, label: 'Editar Produtos' }, { value, label: 'Excluir Produtos' }, { value, label: 'Repor Estoque' }, { value, label: 'Retirar do Estoque' }, { value, label: 'Ver Relatórios' }, { value, label: 'Gerenciar Usuários' }, { value, label: 'Gerenciar Fornecedores' }, { value, label: 'Gerenciar Setores' }, { value, label: 'Gerenciar Categorias' }, { value, label: 'Contagem de Estoque' }, { value, label)' },
]; export default function CadastroSector() { const [sectors, setSectors] = useState<Sector[]>(() => { const saved = localStorage.getItem("sectors"); if (saved) return JSON.parse(saved); return [ { id: 1, name: "Administração", permissions: ['all'] }, { id: 2, name, permissions, 'restock', 'withdraw'] }, ]; }); const [formData, setFormData] = useState({ name, permissions, }); useEffect(() => { localStorage.setItem("sectors", JSON.stringify(sectors)); }, [sectors]); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); const newSector= { id: Math.max(0, ...sectors.map(s => s.id)) + 1, name: formData.name, permissions: formData.permissions, }; setSectors([...sectors, newSector]); resetForm(); alert("Setor cadastrado com sucesso"); }; const resetForm = () => { setFormData({ name, permissions, }); }; const togglePermission = (permission) => { if (permission === 'all') { setFormData({ ...formData, permissions: formData.permissions.includes('all') ? [] }); } else { const filtered = formData.permissions.filter(p => p == 'all'); if (filtered.includes(permission)) { setFormData({ ...formData, permissions: filtered.filter(p => p == permission) }); } else { setFormData({ ...formData, permissions: [...filtered, permission] }); } } }; return ( <div className="p-8"> <div className="mb-8"> <h1 className="text-3xl font-bold text-gray-800 mb-2">Cadastro de Setores</h1> <p className="text-gray-600">Configure setores e suas permissões</p> </div> <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-8 max-w-2xl"> <form onSubmit={handleSubmit} className="space-y-6"> <div> <label className="block text-sm font-medium text-gray-700 mb-2"> Nome do Setor * </label> <input type="text" value={formData.name} onChange={(e) => setFormData({ ...formData, name: e.target.value })} className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none" placeholder="Ex, Estoque, Gerência..." required /> </div> <div> <label className="block text-sm font-medium text-gray-700 mb-3"> Permissões * </label> <div className="space-y-2 max-h-96 overflow-y-auto border border-gray-200 rounded-lg p-4"> {allPermissions.map(perm => ( <label key={perm.value} className={`flex items-center gap-3 p-3 rounded-lg cursor-pointer transition-colors ${ formData.permissions.includes(perm.value) ? 'bg-blue-50 border-2 border-blue-200' : 'bg-gray-50 border-2 border-transparent hover:bg-gray-100' }`} > <input type="checkbox" checked={formData.permissions.includes(perm.value)} onChange={() => togglePermission(perm.value)} className="w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-2 focus:ring-blue-500" /> <span className="text-gray-800">{perm.label}</span> </label> ))} </div> {formData.permissions.length === 0 && ( <p className="text-sm text-red-600 mt-2">Selecione pelo menos uma permissão</p> )} </div> <div className="flex gap-3 pt-4"> <button type="button" onClick={resetForm} className="flex-1 px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50" > Limpar </button> <button type="submit" disabled={formData.permissions.length === 0} className="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:bg-gray-400 disabled:cursor-not-allowed" > Cadastrar Setor </button> </div> </form> </div> </div> );
}
