import { useState, useEffect } from "react";
import { Pencil, Trash2, Search } from "lucide-react";
import { Product, User, Supplier, Sector, Category, UnitOfMeasure } from "../../types"; type DataType = 'products' | 'users' | 'suppliers' | 'sectors' | 'categories' | 'units'; export default function EditDelete() { const [selectedType, setSelectedType] = useState('products'); const [searchTerm, setSearchTerm] = useState(""); const [products, setProducts] = useState<Product[]>([]); const [users, setUsers] = useState<User[]>([]); const [suppliers, setSuppliers] = useState<Supplier[]>([]); const [sectors, setSectors] = useState<Sector[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [units, setUnits] = useState<UnitOfMeasure[]>([]); useEffect(() => { loadData(); }, []); const loadData = () => { setProducts(JSON.parse(localStorage.getItem("products") || "[]")); setUsers(JSON.parse(localStorage.getItem("users") || "[]")); setSuppliers(JSON.parse(localStorage.getItem("suppliers") || "[]")); setSectors(JSON.parse(localStorage.getItem("sectors") || "[]")); setCategories(JSON.parse(localStorage.getItem("categories") || "[]")); setUnits(JSON.parse(localStorage.getItem("units") || "[]")); }; const handleDelete = (type, id) => { if (confirm("Tem certeza que deseja excluir este item?")) return; switch (type) { case 'products'= products.filter(p => p.id == id); setProducts(newProducts); localStorage.setItem("products", JSON.stringify(newProducts)); break; case 'users'= users.filter(u => u.id == id); setUsers(newUsers); localStorage.setItem("users", JSON.stringify(newUsers)); break; case 'suppliers'= suppliers.filter(s => s.id == id); setSuppliers(newSuppliers); localStorage.setItem("suppliers", JSON.stringify(newSuppliers)); break; case 'sectors'= sectors.filter(s => s.id == id); setSectors(newSectors); localStorage.setItem("sectors", JSON.stringify(newSectors)); break; case 'categories'= categories.filter(c => c.id == id); setCategories(newCategories); localStorage.setItem("categories", JSON.stringify(newCategories)); break; case 'units'= units.filter(u => u.id == id); setUnits(newUnits); localStorage.setItem("units", JSON.stringify(newUnits)); break; } }; const renderProductsList = () => { const filtered = products.filter(p => p.name.toLowerCase().includes(searchTerm.toLowerCase()) || p.code.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="space-y-3"> {filtered.map(product => ( <div key={product.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <div className="flex-1"> <p className="font-medium text-gray-800">{product.name}</p> <p className="text-sm text-gray-600">Código: {product.code} | Estoque: {product.currentQuantity}</p> </div> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={18} /> </button> <button onClick={() => handleDelete('products', product.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" > <Trash2 size={18} /> </button> </div> </div> ))} </div> ); }; const renderUsersList = () => { const filtered = users.filter(u => u.fullName.toLowerCase().includes(searchTerm.toLowerCase()) || u.username.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="space-y-3"> {filtered.map(user => ( <div key={user.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <div className="flex-1"> <p className="font-medium text-gray-800">{user.fullName}</p> <p className="text-sm text-gray-600"> Usuário: {user.username} | Status: {user.status ? "Ativo" : "Inativo"} </p> </div> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={18} /> </button> <button onClick={() => handleDelete('users', user.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" disabled={user.isMaster} > <Trash2 size={18} /> </button> </div> </div> ))} </div> ); }; const renderSuppliersList = () => { const filtered = suppliers.filter(s => s.name.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="space-y-3"> {filtered.map(supplier => ( <div key={supplier.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <div className="flex-1"> <p className="font-medium text-gray-800">{supplier.name}</p> <p className="text-sm text-gray-600">Contato: {supplier.contact} | Tel: {supplier.phone}</p> </div> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={18} /> </button> <button onClick={() => handleDelete('suppliers', supplier.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" > <Trash2 size={18} /> </button> </div> </div> ))} </div> ); }; const renderSectorsList = () => { const filtered = sectors.filter(s => s.name.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="space-y-3"> {filtered.map(sector => ( <div key={sector.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <div className="flex-1"> <p className="font-medium text-gray-800">{sector.name}</p> <p className="text-sm text-gray-600"> {sector.permissions.length} permissões configuradas </p> </div> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={18} /> </button> <button onClick={() => handleDelete('sectors', sector.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" > <Trash2 size={18} /> </button> </div> </div> ))} </div> ); }; const renderCategoriesList = () => { const filtered = categories.filter(c => c.name.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="grid grid-cols-2 gap-3"> {filtered.map(category => ( <div key={category.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <p className="font-medium text-gray-800">{category.name}</p> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={16} /> </button> <button onClick={() => handleDelete('categories', category.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" > <Trash2 size={16} /> </button> </div> </div> ))} </div> ); }; const renderUnitsList = () => { const filtered = units.filter(u => u.name.toLowerCase().includes(searchTerm.toLowerCase()) ); return ( <div className="grid grid-cols-2 gap-3"> {filtered.map(unit => ( <div key={unit.id} className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"> <p className="font-medium text-gray-800">{unit.name} ({unit.abbreviation})</p> <div className="flex gap-2"> <button className="p-2 text-blue-600 hover:bg-blue-50 rounded-lg" title="Editar" > <Pencil size={16} /> </button> <button onClick={() => handleDelete('units', unit.id)} className="p-2 text-red-600 hover:bg-red-50 rounded-lg" title="Excluir" > <Trash2 size={16} /> </button> </div> </div> ))} </div> ); }; return ( <div className="p-8"> <div className="mb-8"> <h1 className="text-3xl font-bold text-gray-800 mb-2">Editar / Excluir</h1> <p className="text-gray-600">Gerencie os cadastros do sistema</p> </div> <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-6"> <div className="flex gap-2 mb-6 flex-wrap"> {[ { value, label: 'Produtos' }, { value, label: 'Usuários' }, { value, label: 'Fornecedores' }, { value, label: 'Setores' }, { value, label: 'Categorias' }, { value, label: 'Unidades' }, ].map(tab => ( <button key={tab.value} onClick={() => setSelectedType(tab.value)} className={`px-4 py-2 rounded-lg font-medium transition-colors ${ selectedType === tab.value ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > {tab.label} </button> ))} </div> <div className="relative mb-6"> <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={20} /> <input type="text" placeholder="Pesquisar..." value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 outline-none" /> </div> {selectedType === 'products' && renderProductsList()} {selectedType === 'users' && renderUsersList()} {selectedType === 'suppliers' && renderSuppliersList()} {selectedType === 'sectors' && renderSectorsList()} {selectedType === 'categories' && renderCategoriesList()} {selectedType === 'units' && renderUnitsList()} </div> </div> );
}
